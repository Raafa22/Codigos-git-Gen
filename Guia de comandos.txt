git int - cria repositorio local 

git remote add origin 'link do github

git add .

git commit -m "mensagem de commit"

git push origin main 

agora estamnos na branch nova

git checkout -b dev (isso troucou a branch)